import Layout from "./Layout";

const ManageCatalog = () => {
  return (
    <Layout title="Manage Catalog">
      <div className="text-left">Manage Info</div>
    </Layout>
  );
};

export default ManageCatalog;
